# Partition table for the ESP32-8048S070C (Sunton 7", ESP32-S3, 16MB flash)
#
# Replaces default_8MB.csv, which gave 3.34MB to each app slot and only 1.5MB to
# storage. One full-screen background is 768KB, so that partition held exactly ONE
# picture and the device re-downloaded every time the weather changed.
#
# This firmware is 2.16MB, so 3MB slots leave ~38% headroom for growth - enough
# for an OTA image to land in the other slot - and everything else goes to the
# picture cache. 9.875MB less the 160KB reserve holds about THIRTEEN 768KB
# backgrounds, which covers every weather group plus the owner's own pictures
# without touching the network again.
#
# The framework's own default_16MB.csv would also work but wastes it: it gives
# 6.5MB to each app slot and leaves just 3.4MB for storage (about four pictures).
#
#   Offset       Size        Name
#   0x009000     0x005000    nvs        - WiFi credentials, all Preferences
#   0x00e000     0x002000    otadata    - which app slot to boot
#   0x010000     0x300000    app0       - 3MB, application (OTA slot 0)
#   0x310000     0x300000    app1       - 3MB, application (OTA slot 1)
#   0x610000     0x9e0000    spiffs     - 9.875MB, LittleFS picture cache
#   0xff0000     0x010000    coredump   - crash dump
#
# NOTE: changing this table is a SERIAL flash, not an OTA: the bootloader reads
# the table from 0x8000 and an app image never rewrites it. `pio run -t upload`
# over USB applies it. NVS survives (its offset does not move), the picture cache
# is reformatted, and the first boot after the change re-downloads one picture.
#
# Name,   Type, SubType, Offset,   Size,     Flags
nvs,      data, nvs,     0x9000,   0x5000,
otadata,  data, ota,     0xe000,   0x2000,
app0,     app,  ota_0,   0x10000,  0x300000,
app1,     app,  ota_1,   0x310000, 0x300000,
spiffs,   data, spiffs,  0x610000, 0x9e0000,
coredump, data, coredump,0xff0000, 0x10000,
